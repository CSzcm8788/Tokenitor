# Tokenitor 开发约定

给任何在本仓库工作的人（含 AI 助手）的硬性规则。**这些规则优先于「我觉得这样更好」的判断。**

---

## 0. 这是一个 macOS 原生应用

- **技术栈固定**：Swift + SwiftUI + AppKit，Swift Package Manager 构建（`Package.swift`），
  `build.sh` 手工组装 `.app`。**没有** Xcode 工程文件，不依赖打开 Xcode。
- **禁止**引入任何非原生 UI 方案：Electron / Tauri / WebView 承载界面 / React Native /
  Flutter / 内嵌 HTML+CSS 渲染界面，一律不可。
- **禁止**为了省事把界面改成 Web 技术，或引入跨平台 UI 框架。
- **第三方依赖**：默认零依赖。要新增依赖必须先说明为何系统框架做不到，并获得项目主人同意。
- 界面必须遵循 Apple HIG：系统控件优先（`Form(.grouped)`、`NavigationSplitView`、
  `Toggle`、`Picker`、SF Symbols），自绘只在系统没有对应控件时使用。

---

## 1. 最小改动原则（最重要的一条）

**只改与本次任务直接相关的地方。** 曾多次发生「为修 A 顺手动了 B，结果把 B 弄坏」。

具体要求：

1. **不做未被要求的重构**。看到代码「可以更优雅」——记下来告诉主人，不要顺手改。
2. **不动没坏的东西**。特别是：窗口/材质/布局的既有参数、三端共享组件、发布脚本。
3. **改之前先读懂现状为什么这样写**：本仓库注释里大量记录了「为什么」，多数是踩过坑的结论。
   删掉一行「看起来多余」的代码前，先用 `git log -S'<那行代码>'` 查它是为什么加的。
4. **一次只修一个问题**。修 UI 就只碰 UI；顺手发现的 bug 单独报告、单独提交。
5. **能局部改就不要全局改**。例：某页文字颜色不对 → 改那页；不要去改全局 `Typography`。

### 反面案例（真实发生过）

- 为修「浅色模式工具栏发白」，把 `window.isOpaque = false` 和详情区的
  `VisualEffectView(behindWindow)` 一起删了 → **整个窗口的动态玻璃消失**。
  正确做法：只处理标题栏那一层（`fullSizeContentView` + 透明标题栏让玻璃延伸上去），
  地基三行一个字都不要动。
- 用 `python` 的 `open(p,'w').write(open(p).read()...)` 打补丁 → 写模式先执行，
  文件被清成 0 字节。**永远不要用这个写法**；先读进变量、断言匹配、再写。
- 用正则给 `L("中","英")` 追加文本，结果追加进了英文参数内部 → 中文界面看不到。
  改双语文案时必须确认追加在 `L(...)` **之外**。

---

## 2. 视觉基线：动态玻璃（不要拆）

「动态玻璃」由**两层**共同构成，缺一层就退化成普通灰底：

| 层 | 位置 | 作用 |
|---|---|---|
| **窗口地基** | `AppDelegate.setupWindow()`：`isOpaque = false` + `backgroundColor = .clear` + `styleMask.insert(.fullSizeContentView)` | 窗口自己不画底，且内容延伸到标题栏之下 |
| **内容材质** | `DashboardView` detail 的 `.background(VisualEffectView(material: .popover, blending: .behindWindow).ignoresSafeArea())` | 把桌面模糊透进来，并铺满到顶部 |
| 卡片 | `.glassCard()` → macOS 26+ 用原生 `glassEffect`，旧系统降级 `.ultraThinMaterial` | 每张 AI 卡片自身的玻璃 |

规则：
- **不要**给工具栏（SwiftUI `.toolbarBackground`）指定背景材质。1.3.0 加过
  `.ultraThinMaterial`，浅色模式下呈纯白，把窗口割成「白工具栏 + 灰内容」两截。
- **不要**把窗口改成不透明来「解决」颜色问题。
- `titlebarAppearsTransparent` 必须保持 **false**：标题栏靠自身材质**遮挡滚动到其下的内容**。
  曾设为 true 让玻璃「更连续」，结果正文直接穿过顶栏、与导航标题叠字。
- 顶栏高度由 `window.toolbarStyle` 决定，**不要自绘**：`.unifiedCompact` = 40pt（现用）、
  `.expanded` = 48pt、`.unified` = 66pt、无工具栏 = 28pt。移除全部工具栏按钮后仍需挂一个
  不含 item 的 `NSToolbar`，否则会掉回 28pt。
- 分隔线用系统 `window.titlebarSeparatorStyle = .line`，不要用 `safeAreaInset` 自绘
 （那会追加安全区、把内容顶下去，实测导致标题与卡片重叠）。
- 改任何材质/背景/高度后，**浅色与深色都要各截一张图确认**。

---

## 3. 三端一致

同一份数据出现在三处：**主窗口仪表（hero）/ 菜单栏弹层 / 刘海面板**。

- 胶囊行 → `ProviderChipsRow`；卡片头 → `ProviderCardHeader`；用量条 → `UsageBar`。
- 要改这三处的观感，**只改共享组件**，不要在某一端就地写死；否则三端立刻长歪。
- 改完必须三端各截一张图确认（刘海面板窄，最容易先崩）。

### 3.1 刷新是全自动的（不要加回手动按钮）

界面上**没有**手动刷新按钮，这是有意的：定时器（默认 120s，`.common` mode + tolerance +
关闭 App Nap）+ 打开主窗口/弹层时数据超 30s 补刷 + **系统唤醒后补刷**（睡眠期间 `Timer`
完全不触发）。手动路径保留 ⌘R / 弹层「刷新」/ 菜单栏右键三条，都会清除限流退避并全量重试。

不要为「让用户看到正在刷新」而新增没有观察者的 `@Published`——那样每轮会白触发两次
SwiftUI 重绘（`isRefreshing` 就是这样被移除的）。并发保护用 `AppDelegate.isFetching`。

## 4. 每次改动都要同步文档与双语（硬性）

**任何功能或 UI 改动，都必须在同一次改动里把文档与双语一起更新完**——不允许「先改代码，文档下次补」。
曾多次出现界面已变而 README / 说明页还写着旧行为、或中文改了英文没改的情况。

### 4.1 每次改动的同步清单

| 改了什么 | 必须同步 |
| :--- | :--- |
| 任何界面文案 | 中英两侧（`L("中","英")` 成对） |
| 任何 UI 行为 / 布局 | 应用内**说明页**对应条目 + `README.md` 正文对应章节 + `README.en.md` 同一章节 |
| 任何功能（新增/移除/改口径） | 上述全部 + `README.md` 更新日志 + 关于页 `releaseNotes`（最近 3 条） |
| UI 外观有变 | 重拍 `docs/` 相关截图 |
| 数据来源 / 口径 | 说明页数据源卡 + README 数据表 + `AIKind.coverage`（若涉及覆盖范围） |

**中英对齐不是「翻译一遍」**：`README.en.md` 的章节结构要与 `README.md` 正文保持一一对应
（唯一例外：英文版**刻意不放更新日志**，完整日志只在中文 README 与 GitHub Releases）。
改中文正文时若英文缺对应章节，就要补上，不能只改一边。

### 4.2 双语写法

- 全部界面文案用 `L("中文", "English")` 就地成对写，无字符串表。
- 新增文案**必须**同时给两种语言；改文案两边一起改。
- 追加文本时注意括号位置：`L("中","英") + 后缀`（对），`L("中","英" + 后缀)`（错——后缀只进了英文，
  中文界面看不到；实测踩过）。
- 中文界面文案在设置页遵循 **4 个汉字**的标签规范（见 §3 的对齐要求）；说明性文字不受此限。

### 4.3 自检

提交前逐条问自己：**说明页说的还对吗？README 正文说的还对吗？英文那边动了吗？截图还是这个样子吗？**
四个都答"是"才算改完。

## 5. 数据可信度（口径）

- **宁可不显示，也不显示错的**。字段缺失/解析失败 → 如实报「读取失败」，
  绝不兜底成「剩余 100%」这类看起来正常的值。
- 缓存有有效期；过期不再当用量显示。展示旧数据时必须让用户看得出来（`缓存` 胶囊 / 「数据 X前」）。
- 各源百分比的**覆盖范围**不同，写在 `AIKind.coverage`，卡片悬停与说明页共用同一份文案。
- 档位胶囊只在本地读到的值能映射到**真实档位**时显示（白名单，见 `PlanTier`）。

## 6. 隐私与合规

- 只读本机属于用户自己的数据与凭证，只读不改、零上传、无自有服务器。
- Claude / Copilot 走社区通用接口（官方未文档化）：**默认关闭**，开启前各弹一次风险确认。
- 诚实 `User-Agent`，**不伪装官方客户端**（即使能换来更宽松的限流）。
- 测试 fixture 必须**白名单脱敏**：只保留解析器读取的字段与数字，不含对话内容 / 会话 ID / 凭证。

## 7. 性能纪律

- 会话文件可达数百 MB、单行可达数 MB。读取一律**增量 + 流式**：记字节偏移、只扫新增、
  巨行先用 `contains` 预筛再解析、每行一个 `autoreleasepool`。
- 超大文件首扫从尾部预算起读（今日数据必在尾部）。
- 改动任何读取路径后，实测冷启动内存峰值并与上版对比（历史基线：Token 页首扫 264MB、
  关窗常驻 47MB）。

## 8. 验证与发布

- **本机验证优先**：`swift build` → `swift test` → `bash install.sh` → 实机看效果。
  验证没过不提交。
- UI 改动必须截图确认（工具：`CGWindowListCopyWindowInfo` 取窗口 ID + `screencapture -l`）。
  临时用的 env 钩子/调试补丁**提交前必须撤净**（`grep -rn "TEMP" Sources/`）。
- **公证前必须先向项目主人确认**（Apple 建议每账号 ≤75 次/天；`--wait` 连接超时
  **不等于**失败，任务已在排队，重复提交只会白耗额度——改用 `notarytool info` 轮询）。
- 版本号两处：`build.sh` 的 `VERSION` 与 `Branding.swift` 的兜底值。
- 发版前再走一遍 §4 的同步清单（发版是最后一道关，不是第一次同步的时机）。
- 改 `release.sh` 后先 `bash -n release.sh` 查语法。

## 9. 仓库禁忌

- 任何文件不得出现同类竞品的具体项目名（对比分析只在对话里做）。
- 不打包任何第三方品牌 logo；AI 一律只用名称文字标识。
